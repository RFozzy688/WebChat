namespace WebChatCore
{
    /// <summary>
    /// Типы элементов для пункта меню
    /// </summary>
    public enum MenuItemType
    {
        // Показывает текст меню и значок
        TextAndIcon = 0,

        // Показывает простой разделитель между пунктами меню
        Divider = 1,

        // Показывает текст меню в виде заголовка
        Header = 2
    }
}
